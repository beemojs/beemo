{
  "private": true,
  "name": "beemo-root",
  "scripts": {
    "babel": "beemo run-script build-packages",
    "build": "yarn run babel",
    "build:dts": "beemo run-script generate-dts",
    "clean": "rimraf ./packages/*/{lib,esm}/ && lerna clean --yes",
    "clean:cache": "rimraf ./cache",
    "coverage": "yarn run jest --coverage",
    "docs": "gitbook build --debug --log=debug",
    "docs:serve": "gitbook serve",
    "docs:install": "gitbook install",
    "eslint": "beemo eslint",
    "jest": "beemo jest",
    "posttest": "yarn run eslint",
    "pretest": "yarn run type",
    "test": "yarn run jest",
    "prettier": "beemo prettier",
    "package": "yarn run clean && yarn run build && yarn test",
    "prerelease": "yarn run package",
    "release": "lerna publish",
    "release:force": "yarn run release --force-publish=*",
    "type": "beemo typescript --noEmit --workspaces"
  },
  "devDependencies": {
    "@milesj/build-tool-config": "0.82.2",
    "chalk": "^2.3.2",
    "fs-extra": "^5.0.0",
    "glob": "^7.1.2",
    "lerna": "3.0.0-beta.3",
    "semver": "^5.5.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "workspaces": [
    "packages/*"
  ],
  "beemo": {
    "module": "@local",
    "drivers": [
      "babel",
      "eslint",
      {
        "driver": "jest",
        "env": {
          "NODE_ENV": "test"
        }
      },
      "mocha",
      "prettier",
      "typescript"
    ],
    "eslint": {
      "rules": {
        "function-paren-newline": "off",
        "import/first": "off"
      }
    }
  }
}
