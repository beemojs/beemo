import { TypeScriptConfig } from '@beemo/driver-typescript';
import baseConfig from '@milesj/build-tool-config/src/configs/typescript';

const config: TypeScriptConfig = {
  ...baseConfig,
};

export default config;
